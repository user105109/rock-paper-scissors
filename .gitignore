# Ignore everything by default
/*

# Don't ignore this .gitignore so it can be committed
!.gitignore

# Allow project files you care about
!/FlexBox.html
!/flexbox.css

# Keep workspace settings tracked so git settings persist
!/.vscode/
!/.vscode/settings.json

# (Optional) add exceptions for other files you create later
# !/path/to/other-file.ext
